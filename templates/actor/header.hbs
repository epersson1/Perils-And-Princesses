{{! Sheet Header }}
<header class='sheet-header'>
  <img
    class='profile-img'
    src='{{actor.img}}'
    data-edit='img'
    data-action='onEditImage'
    title='{{actor.name}}'
    height='100'
    width='100'
  />
  <div class='header-fields'>
    <div class='document-name'>
      <input
        name='name'
        type='text'
        value='{{actor.name}}'
        placeholder='Name'
      />
    </div>
    {{! The grid classes are defined in scss/global/_grid.scss. To use,
      use both the "grid" and "grid-Ncol" class where "N" can be any number
      from 1 to 12 and will create that number of columns.  }}
    <div class='resources grid grid-3col'>

      <div class="resource flex-group-center">
          <button type="button" data-action="takePicnic">Take a Picnic</button>
          <button type="button" data-action="takeRest">Take a Rest</button>
        </div>
      {{! "flex-group-center" is also defined in the _grid.scss file
        and it will add a small amount of padding, a border, and will
        center all of its child elements content and text. }}
      <div class='resource flex-group-center'>
        <label for="system.pronouns" class="resource-label">
          Pronouns
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.pronouns'
            value='{{system.pronouns}}'
            data-dtype='text'
          />
        </div>
      </div>

      <div class='resource flex-group-center'>
        <label for="system.kingdom" class="resource-label">
          Kingdom
        </label>
        <div class='resource-content flexrow flex-center flex-between'>
          <input
            type='text'
            name='system.kingdom'
            value='{{system.kingdom}}'
            data-dtype='text'
          />
        </div>
      </div>
      </div>
    </div>
  </div>
</header>